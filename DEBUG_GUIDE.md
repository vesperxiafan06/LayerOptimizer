# 调试指南：为什么节点无法移除

## 如何查看调试信息

### 1. 打开 Figma 开发者控制台
1. 在 Figma 中，按 `Cmd+Option+I` (Mac) 或 `Ctrl+Shift+I` (Windows) 打开开发者工具
2. 切换到 "Console" 标签页
3. 运行插件并点击优化
4. 查看控制台输出的错误和警告信息

### 2. 查看错误信息类型

#### 错误类型1：节点不再存在
```
Node no longer exists or has no parent
```
**原因**：节点在扫描和优化之间被删除或移动了

#### 错误类型2：节点不再满足冗余条件
```
Node no longer meets redundant container criteria
```
**原因**：节点在优化时不再满足冗余容器的所有条件

#### 错误类型3：移除操作失败
```
Failed to remove node (unwrapNode returned false)
```
**原因**：`unwrapNode` 函数返回 false，可能的原因：
- 节点没有父节点
- 节点没有子节点
- 节点不在父节点的子节点列表中
- `insertChild` 操作失败
- `remove` 操作失败

#### 错误类型4：节点不在选中的问题列表中
```
Node not in selected issue list
```
**原因**：节点ID不在 `selectedIssueIds` 集合中（不应该发生，但为了安全起见）

## 常见问题排查

### 问题1：节点在递归过程中被跳过
**症状**：节点被识别为冗余，但在优化时没有被移除

**可能原因**：
1. 节点的子节点先被移除，导致节点不再满足冗余条件
2. 节点在递归过程中状态改变（比如尺寸、位置等）

**解决方案**：
- 查看控制台的警告信息
- 检查节点是否仍然满足所有冗余条件

### 问题2：节点找不到根节点
**症状**：节点被识别为冗余，但优化时显示 "Node no longer exists"

**可能原因**：
1. 节点在扫描后被移动到了其他位置
2. 节点被删除后重新创建（ID改变）

**解决方案**：
- 代码已经修复：如果找不到根节点，会直接处理该节点
- 如果仍然失败，查看控制台的错误信息

### 问题3：unwrapNode 失败
**症状**：控制台显示 "Failed to remove node"

**可能原因**：
1. 节点没有父节点（不应该发生）
2. 节点没有子节点（不应该发生，因为已经检查过）
3. `insertChild` 操作失败（可能是权限问题）
4. `remove` 操作失败（可能是节点被锁定）

**解决方案**：
- 检查节点是否被锁定
- 检查节点是否有特殊权限限制
- 查看控制台的详细错误信息

## 调试步骤

1. **打开控制台**：按 `Cmd+Option+I` 打开开发者工具
2. **运行扫描**：点击"开始扫描"按钮
3. **查看扫描结果**：确认节点被识别为冗余
4. **运行优化**：点击"一键优化"按钮
5. **查看控制台**：检查是否有错误或警告信息
6. **查看错误详情**：根据错误信息定位问题

## 代码改进

### 已添加的改进：
1. ✅ 详细的错误处理和日志
2. ✅ 验证节点是否仍然有效
3. ✅ 直接处理找不到根节点的问题节点
4. ✅ 检查节点是否仍然满足冗余条件
5. ✅ 详细的 `unwrapNode` 错误处理

### 调试信息位置：
- `optimizeNodeRecursively`: 检查节点是否仍然冗余
- `unwrapNode`: 详细的错误处理和日志
- `handleOptimize`: 处理找不到根节点的情况


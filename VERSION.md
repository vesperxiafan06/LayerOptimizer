# Layer Optimizer - MVP Version

## Version: 1.0.0 (MVP)

### 核心功能

#### 1. 智能扫描冗余图层
- 自动检测 Frame 和 Group 类型的冗余容器
- 识别无视觉效果的容器（无填充、无描边、无效果）
- 支持 Auto Layout 环境下的冗余检测
- 自底向上递归扫描，确保安全优化

#### 2. 选择性优化
- 复选框控制，可选择要优化的项目
- 支持批量优化多个问题
- 优化结果仅在 Figma 画布通知中显示

#### 3. 图层定位功能
- 点击问题项可定位到对应图层
- 复选框和定位功能分离，交互清晰

#### 4. 用户界面
- 简洁现代的 UI 设计
- 中英文双语支持（默认英文）
- 语言切换功能
- 标题：Layer Optimizer
- 进度条显示扫描进度

### 技术特性

#### 安全优化算法
- 严格的安全检查机制
- 保护组件和实例
- 保护 Auto Layout 的 padding 和约束
- 保持设计元素的样式、尺寸和位置不变
- 坐标补偿确保视觉位置 100% 不变

#### 优化逻辑
- 自底向上（Bottom-Up）遍历策略
- 先处理最里面的子孙节点，再处理当前节点
- 根据 checkbox 状态决定优化范围，不依赖 Figma selection
- 保存扫描时的根节点，确保优化范围准确

#### 检测规则
冗余容器必须满足以下所有条件：
1. 类型为 Frame 或 Group
2. 有且只有 1 个子元素
3. 不是组件或实例
4. 父级不是实例
5. 尺寸与子元素一致（允许 1px 误差）
6. 子元素在 (0,0) 位置（针对 Frame）
7. 无视觉效果（无填充、描边、效果）
8. 无 padding（针对 Auto Layout）

### UI/UX 特性

#### 界面布局
- 顶部：标题 "Layer Optimizer" + 语言切换按钮
- 初始状态：开始扫描按钮
- 扫描中：进度条 + 加载状态
- 结果页面：
  - 问题列表（带复选框）
  - 优化摘要
  - 一键优化 / 重新扫描按钮

#### 交互设计
- Checkbox 热区独立，点击切换选中状态
- 图层名称可点击定位到图层
- Tooltip 显示优化原因（悬停 "?" 图标）
- 问题项高度 32px，布局紧凑

#### 语言支持
- 默认语言：英文
- 支持语言：English / 中文
- 所有 UI 文本完整翻译
- 后端消息自动翻译

### 文件结构

```
图层优化/
├── manifest.json      # 插件配置
├── code.js           # 核心逻辑（扫描、优化算法）
├── ui.html           # 用户界面
├── README.md         # 使用说明
└── VERSION.md        # 版本记录（本文件）
```

### 已知限制

1. Figma 通知使用英文（无法获取前端语言设置）
2. 仅支持 Figma 编辑器
3. 需要手动选择要扫描的图层范围

### 未来可能的改进方向

- [ ] 支持批量扫描整个页面
- [ ] 添加撤销/重做支持
- [ ] 优化历史记录
- [ ] 更多语言支持
- [ ] 自定义优化规则
- [ ] 导出优化报告

---

**创建日期**: 2024
**MVP 状态**: ✅ 完成
**测试状态**: 待测试


